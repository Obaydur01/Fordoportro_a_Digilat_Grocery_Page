// Seed data for Fordoportro — used by POST /api/admin/seed
// Products, categories, and employees are managed via MongoDB and admin panel only.

const categories = [];

const products = [];

const employees = [];

module.exports = { categories, products, employees };
